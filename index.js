/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { DpromApp } from './src/DpromApp';

AppRegistry.registerComponent(appName, () => DpromApp);
